const INITIAL_STATE = {
    name: "unknown",
    surname: "unknown"
}

export default function(state = INITIAL_STATE, action) {
    switch(action.type) {
        default: 
            return state
    }
}
